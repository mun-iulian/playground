name: Main pipeline

on:
  push:
    branches:
      - '**'

jobs:
  main:
    name: Main
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Hello world
        run: 
          echo "This CI is running on branch ${{ github.ref }}"

      - name: Run super duper secret command if running on master
        if: '${{ github.ref }}' == 'refs/heads/main'
        run:
          echo "Super duper secret command"
